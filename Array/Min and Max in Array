class Solution:
    def getMinMax(self, arr):
        # optimized approach - 1
        min = arr[0]
        max = arr[0]
        
        for i in arr:
            if i < min:
                min = i 
            if i > max:
                max = i
        return min, max


        # Approach - 2  
          arr.sort()
          for i in arr:
              min, max = arr[0], arr[len(arr)-1]
          return min, max 
  
